<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <LangVersion>latest</LangVersion>
    <RootNamespace>Source2Boost.App</RootNamespace>
    <AssemblyName>Source2Boost</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Product>Source2Boost</Product>
    <Version>1.2.0</Version>
    <Company>Source2Boost</Company>
    <ApplicationIcon>icon.ico</ApplicationIcon>
    <!-- WFAC010: WinForms предлагает вынести DPI из манифеста, но манифест нужен WPF — глушим. -->
    <NoWarn>$(NoWarn);WFAC010</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <Resource Include="icon.ico" />
  </ItemGroup>

  <!-- WinForms нужен только для значка в трее (System.Windows.Forms.NotifyIcon). Убираем его
       неявные global usings, иначе они конфликтуют с WPF-типами (Application, MessageBox, Color,
       Brush, Point и т.д.) по всему проекту. WinForms-типы в коде пишем полным именем. -->
  <ItemGroup>
    <Using Remove="System.Windows.Forms" />
    <Using Remove="System.Drawing" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Source2Boost.Core\Source2Boost.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="WPF-UI" Version="4.3.0" />
  </ItemGroup>

  <!-- Кладём PresentMon 2.x рядом с exe при сборке/публикации, чтобы dev-запуск (bin\Debug)
       брал ПРАВИЛЬНУЮ версию из своей папки, а не старый tools\PresentMon.exe через фолбэк. -->
  <ItemGroup>
    <None Include="..\..\tools\PresentMon-2.5.1-x64.exe" Link="PresentMon.exe">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
